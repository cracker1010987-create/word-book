# wordbook 스펙

## 1. 사용자와 사용 시점
- 나 혼자 쓰는 개인용 CLI 도구
- 단어를 외울 때마다 사용

## 2. 핵심 기능 3개
1. 단어 추가
2. AI가 퀴즈 내주기
3. 틀린 단어를 다시 물어보기

## 3. 데이터 저장 방식
- JSON 파일 하나 (예: `words.json`)
- 데이터베이스 사용 안 함
- 단어별로 최소 `단어`, `뜻`, `틀린 횟수`(또는 정답/오답 기록) 저장

## 4. LLM 사용 지점 (정확히 한 곳)
- 퀴즈 문제를 생성하는 곳 한 군데에서만 `ChatOpenAI` 사용
- 정답 채점은 LLM 호출 없이 코드(문자열 비교)로 처리

## 5. CLI 명령어 목록
- `wordbook add <단어> <뜻>` — 단어 추가
- `wordbook quiz` — AI가 퀴즈 출제 (틀렸던 단어 우선 출제)
- `wordbook stats` — 통계(정답률, 틀린 단어 등) 확인

## 6. 이번에 안 만들 것
- 웹 화면
- 회원가입 / 로그인
- 발음 듣기
- 모바일 앱
- 여러 사람 같이 쓰기 (멀티유저)
- RAG 검색

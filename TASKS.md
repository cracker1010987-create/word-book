# TASKS

prd.md를 기준으로 앱을 테스트 가능한 크기의 기능 조각으로 쪼갠 목록입니다.
순서(④)는 "앞 조각이 없으면 만들 수 없는 것"을 뒤로 배치한 권장 빌드 순서입니다.
코드는 아직 작성하지 않습니다.

**확정 사항**: "틀린 단어 우선 출제"는 틀린 횟수만 비교하는 방식(a)으로 간다.
날짜 기반 복습 주기(스페이스드 리피티션)는 만들지 않는다.
`today`는 출제 순서 결정에는 쓰이지 않고, `record_result`가 "마지막으로 틀린 날짜"를
기록해두는 용도로만 쓰인다(나중에 stats에서 보여줄 수 있게).

**업데이트 (2026-09-13)**: `record_result`가 맞히면 `wrong_count`를 1 내리도록 바뀜(0 밑으로는
안 내려감) — 계속 맞히는 단어는 우선순위가 낮아져서 다른 단어로 자연스럽게 넘어가게 하기 위함.
(보기 번호로 답하는 기능은 한 번 추가했다가 되돌림 — 영어 단어 타이핑만 정답으로 인정.)

---

### 1. `check_answer()` — 정답 채점하기
- **파일**: `grading.py`
- **검사 방법**: pytest (대소문자·공백 차이 등 케이스 확인)
- **순서**: 1번째. 선행 조각 없음 (입력값만으로 동작하는 순수 함수).

### 2. `pick_word_to_quiz(words)` — 출제할 단어 고르기
- **파일**: `review.py`
- **검사 방법**: pytest (틀린 횟수가 많은 단어가 우선 선택되는지 확인)
- **순서**: 1번째. 선행 조각 없음 (틀린 횟수만 보는 순수 함수, 날짜 불필요).

### 3. ✅ `record_result(words, word, is_correct, today)` — 채점 결과 반영하기
- **파일**: `review.py`
- **검사 방법**: pytest (틀린 횟수 갱신 + `today`가 마지막 틀린 날짜로 저장되는지 확인)
- **순서**: 2번째. 선행 조각: 2 (같은 단어 데이터 구조를 다룸).

### 4. ✅ `load_words()` — 단어 데이터 불러오기
- **파일**: `storage.py`
- **검사 방법**: pytest (파일이 있을 때 / 없을 때 두 경우 확인)
- **순서**: 1번째. 선행 조각 없음.

### 5. ✅ `save_words()` — 단어 데이터 저장하기
- **파일**: `storage.py`
- **검사 방법**: pytest (저장 후 파일을 다시 읽어 내용이 같은지 확인)
- **순서**: 1번째. 선행 조각 없음.

### 6. ✅ `add_word()` — 단어 추가하기
- **파일**: `storage.py`
- **검사 방법**: pytest
- **순서**: 2번째. 선행 조각: 4, 5.

### 7. ✅ `calculate_stats(words)` — 통계 계산하기
- **파일**: `stats.py`
- **검사 방법**: pytest
- **순서**: 3번째. 선행 조각: 3 (마지막 틀린 날짜·틀린 횟수 필드가 채워져 있어야 함).

### 8. ✅ `build_quiz_chain()` — LCEL 퀴즈 체인 만들기 (실제 함수명: `build_chain()`)
- **파일**: `ai.py`
- **검사 방법**: pytest (체인이 예상한 구조로 만들어지는지만 확인, 실제 API 호출 없음)
- **순서**: 1번째. 선행 조각 없음.

### 9. ✅ `generate_quiz_question(chain, word, meaning)` 배선 로직 (실제 함수명: `make_quiz(word, meaning, chain=None)`)
- **파일**: `ai.py`
- **검사 방법**: pytest (가짜 체인을 주입해서 함수가 체인을 올바른 인자로 호출하고 결과를 그대로 돌려주는지 확인)
- **순서**: 2번째. 선행 조각: 8.

### 10. ✅ 실제 퀴즈 문제 품질 확인 (`generous` PASS, `elaborate`는 알려진 예외로 `samples/quiz-elaborate.json`에 기록)
- **파일**: `ai.py` (실제 `ChatOpenAI` 호출 결과 확인)
- **검사 방법**: AI 심판 (진짜 API로 생성된 문제가 단어 뜻과 맞는 퀴즈인지 판단)
- **순서**: 3번째. 선행 조각: 9.
- **보완**: `check_grammar`/`make_quiz_verified`(`ai.py`)로 "빈칸 뒤 전치사가 정답만 가리키는"
  문제를 자동 검사 후 재시도하게 만듦. 실제로 나쁜 문장을 잡아서 재생성시키는 건 확인됐지만,
  판정 AI 자체의 판단이 가끔 관대해서 100% 확정적으로 막지는 못하는 **알려진 한계**로 남겨둠.

### 11. ✅ `wordbook add` 명령어 연결
- **파일**: `__main__.py`
- **검사 방법**: pytest (`main()` 함수를 직접 호출, 임시 파일로 words.json에 반영됐는지 확인)
- **순서**: 3번째. 선행 조각: 6.

### 12. ✅ `wordbook quiz` 명령어 배선
- **파일**: `__main__.py`
- **검사 방법**: pytest (가짜 체인 주입, 단어 선택 → 채점 → 결과 반영까지 흐름만 확인)
- **순서**: 4번째. 선행 조각: 1, 2, 3, 9.

### 13. ✅ `wordbook quiz` 실제 사용 흐름 확인
- **파일**: `__main__.py` (실제 LLM 연결 상태로 실행)
- **검사 방법**: AI 심판 (실제로 써봤을 때 퀴즈가 자연스럽고 채점이 맞는지 판단)
- **순서**: 5번째. 선행 조각: 10, 12.

### 14. ✅ `wordbook stats` 명령어 연결
- **파일**: `__main__.py`
- **검사 방법**: pytest
- **순서**: 4번째. 선행 조각: 7.

### 15. ✅ (추가) 대화형 메뉴 모드
- **파일**: `__main__.py` (`run_interactive`)
- **검사 방법**: pytest
- **순서**: 5번째. 선행 조각: 11, 12, 14. (명령어 인자 방식이 불편하다는 피드백으로 추가)

### 16. ✅ (추가) 단어 없을 때 quiz 크래시 방지
- **파일**: `__main__.py` (`run_quiz`)
- **검사 방법**: pytest
- **순서**: 6번째. 선행 조각: 12. (`words`가 비어있으면 `pick_word_to_quiz`가 `ValueError`를
  던지던 버그 발견 → 안내 메시지 출력 후 조용히 리턴하도록 수정)

### 17. ✅ (추가) 이미 있는 단어를 다시 추가할 때 기록 보존
- **파일**: `storage.py` (`add_word`), `__main__.py` (`run_add`)
- **검사 방법**: pytest
- **순서**: 6번째. 선행 조각: 6. (기존엔 재추가 시 `wrong_count`가 조용히 0으로 리셋되던 버그
  발견 → 이미 있는 단어면 뜻만 바꾸고 `wrong_count`/`last_wrong_date`는 보존, 새 단어인지
  여부를 화면에 알려줌)

### 18. ✅ (추가) `wordbook list` — 추가한 단어 목록 보기
- **파일**: `__main__.py` (`print_word_list`)
- **검사 방법**: pytest
- **순서**: 6번째. 선행 조각: 6. (메뉴 4번=목록 보기, 5번=종료로 재배치)

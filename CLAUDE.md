# CLAUDE.md

이 파일은 이 프로젝트에서 작업할 때 항상 지켜야 하는 규칙입니다.
사용자는 파이썬 입문자입니다. 설명은 쉽게, 코드는 짧고 읽기 쉽게 작성하세요.

## 프로젝트

- **wordbook**: 영어 단어를 저장하고 AI가 퀴즈를 내주는 CLI 앱
- 코드는 짧고 읽기 쉽게 작성한다. **한 함수는 20줄 이내**로 유지한다.

## 기술 스택 (이것만 사용)

- 파이썬 표준 라이브러리
- `langchain`, `langchain-openai` (LLM은 `ChatOpenAI`, 모델은 `gpt-5.6-luna`)
- `python-dotenv`, `pytest`
- **새 라이브러리가 필요하면 먼저 사용자에게 물어본다. 마음대로 추가하지 않는다.**

## 코드 규칙

- 모든 함수에 **한국어 한 줄 주석**으로 "이 함수가 뭐 하는지" 설명한다.
- 모든 함수에 **타입 힌트**를 붙인다.
- 데이터 저장은 **JS*ON 파일 하나*로 한다. 데이터베이스는 사용하지 않는다.
- 랭체인 체인은 **LCEL 문법 (`|` 파이프)** 으로 작성한다.

## 훅(hook) 위반 대응

- `.claude/checks/check.py`는 `.py` 파일을 쓰거나 고칠 때마다 `PostToolUse` 훅으로 자동 실행된다.
- 이 훅은 **파일이 이미 저장된 뒤에** 실행되므로, 실패해도 파일을 자동으로 되돌려주지 않는다.
- 그래서 훅이 실패(exit 2)를 알려주면, 설명만 하고 넘어가지 말고 **그 자리에서 바로 원인을 고치거나 방금 만든 파일을 되돌린다.**

## 절대 금지

- API 키를 코드 안에 직접 쓰지 않는다. 반드시 `.env` 파일 + `load_dotenv()` 로 읽는다.
- 테스트가 실패한 상태에서 "완료"라고 말하지 않는다.
- 사용자가 요청하지 않은 기능을 미리 만들지 않는다. 먼저 물어본다.
- 한 번에 여러 기능을 동시에 만들지 않는다. **하나씩** 작업한다.

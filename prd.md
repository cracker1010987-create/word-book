# wordbook PRD

이 문서는 이 프로젝트의 단일 기준 문서입니다. spec.md의 내용을 바탕으로 작성했습니다.

## ① 개요

- 영어 단어를 저장하고 AI가 퀴즈를 내주는 개인용 CLI 도구다.
- 나 혼자, 단어를 외울 때마다 사용한다.
- 단어 추가 → AI 퀴즈 출제 → 틀린 단어 재출제의 흐름으로 동작한다.

## ② 기능 목록

1. **단어 추가** — `wordbook add <단어> <뜻>`으로 단어를 저장한다.
2. **AI 퀴즈** — `wordbook quiz`로 LLM이 퀴즈 문제를 내고, 채점은 코드로 처리한다. 틀렸던 단어를 우선 출제한다.
3. **틀린 단어 재출제** — 퀴즈에서 틀린 단어는 다음 퀴즈에서 우선적으로 다시 나온다.
4. **통계 보기** — `wordbook stats`로 정답률, 틀린 단어 목록 등을 확인한다.

## ③ 기술 결정

### 결정 1. 데이터는 `words.json` 파일 하나에 저장한다

- **상황**: 단어, 뜻, 틀린 횟수 등 앱의 모든 데이터를 어딘가에 저장해야 한다. CLAUDE.md 규칙상 데이터베이스는 쓸 수 없고, 입문자가 직접 열어서 확인·수정할 수 있는 형태여야 한다.
- **결정**: 모든 단어 데이터를 `words.json` 파일 하나에 저장한다.
- **검토한 대안**:
  - **SQLite 데이터베이스** — 거부 이유: CLAUDE.md에서 DB 사용을 금지했고, 입문자가 SQL 없이는 내용을 직접 열어보거나 수정하기 어렵다.
  - **CSV 파일** — 거부 이유: 틀린 횟수처럼 단어마다 여러 값을 담는 중첩 구조를 표현하기 불편하고, 나중에 필드가 늘어나면 열 구조를 계속 바꿔야 한다.
  - **단어별로 파일을 따로 만들기 (예: `words/apple.json`)** — 거부 이유: 파일이 단어 수만큼 늘어나 관리가 번거롭고, "전체 단어 목록"이나 "통계"를 보려면 모든 파일을 순회해야 해서 오히려 코드가 복잡해진다.
- **결과**:
  - 장점: 파이썬 표준 라이브러리(`json`)만으로 읽고 쓸 수 있고, 파일 하나만 보면 전체 데이터를 한눈에 파악할 수 있다. 백업도 파일 하나만 복사하면 된다.
  - 단점: 단어 수가 아주 많아지면 매번 파일 전체를 읽고 다시 쓰는 방식이라 느려질 수 있고, 여러 프로세스가 동시에 파일을 수정하면 데이터가 깨질 위험이 있다(다만 혼자 쓰는 CLI 도구라 지금은 문제 되지 않는다).

### 결정 2. LLM 호출은 `ai.py` 한 곳에만 두고, 체인은 함수 밖에서 만들어 인자로 전달한다

- **상황**: spec.md에서 "LLM을 쓰는 부분은 정확히 한 곳"으로 정했다. 이를 코드로 지키려면 LLM 호출 코드가 여러 파일에 흩어지지 않도록 강제할 규칙이 필요하고, 테스트할 때는 실제 OpenAI API를 호출하지 않고 가짜 체인으로 바꿔 끼울 수 있어야 한다.
- **결정**: `ChatOpenAI`와 LCEL 체인 생성 코드는 `ai.py`에만 둔다. 체인 객체는 함수 안에서 직접 만들지 않고, 호출하는 쪽에서 만들어 함수 인자로 넘긴다.
- **검토한 대안**:
  - **퀴즈 함수 안에서 매번 체인을 새로 만들기** — 거부 이유: 함수를 테스트할 때 진짜 체인만 쓸 수 있어 API 키 없이는 테스트가 불가능하고, 호출할 때마다 체인 객체를 새로 생성해 불필요한 비용이 든다.
  - **quiz.py, stats.py 등 필요한 곳마다 각자 `ChatOpenAI`를 호출하기** — 거부 이유: spec의 "정확히 한 곳" 규칙을 어기게 되고, 나중에 모델이나 프롬프트를 바꿀 때 여러 파일을 찾아다니며 고쳐야 한다.
  - **전역 변수로 체인 하나를 만들어두고 아무 데서나 가져다 쓰기** — 거부 이유: 모듈을 임포트하는 순간 API 키를 읽어 체인이 생성되므로 테스트 환경에서 API 키가 없으면 임포트 자체가 실패할 수 있고, 어떤 함수가 이 전역 상태에 의존하는지 코드만 봐서는 알기 어렵다.
- **결과**:
  - 장점: 테스트에서는 진짜 체인 대신 가짜(fake) 체인을 인자로 넣어 API 호출 없이 로직을 검증할 수 있다. 모델이나 프롬프트를 바꿀 일이 생겨도 `ai.py` 한 파일만 고치면 된다.
  - 단점: 체인을 쓰는 함수를 호출할 때마다 체인 객체를 인자로 넘겨야 해서 호출부 코드가 한 줄 더 길어지고, 실제 앱을 실행하는 진입점(`main` 등)에서 체인을 조립해 전달하는 코드를 따로 챙겨야 한다.

### 결정 3. '오늘 날짜'는 함수 안에서 구하지 않고 항상 `today` 인자로 받는다

- **상황**: 틀린 단어를 우선 출제하거나 통계를 계산할 때 '오늘 날짜'가 필요할 수 있다. 함수가 내부에서 직접 현재 시각을 구하면, 실행할 때마다 날짜가 달라져 테스트 결과를 예측할 수 없다.
- **결정**: 날짜가 필요한 함수는 `today`라는 이름의 파라미터로 날짜를 받고, 함수 안에서는 절대 현재 날짜를 직접 구하지 않는다.
- **검토한 대안**:
  - **함수 내부에서 `datetime.now()` 또는 `date.today()`를 직접 호출하기** — 거부 이유: 실행 시각에 따라 결과가 달라져 같은 입력으로 같은 결과를 확인해야 하는 테스트를 재현할 수 없다.
  - **오늘 날짜를 전역 변수나 설정값으로 관리하기** — 거부 이유: 여러 함수가 같은 전역 상태를 몰래 참조하게 되어 코드만 보고는 날짜 의존성을 알기 어렵고, 결국 그 전역값을 채우는 시점에는 여전히 `datetime.now()` 문제가 남는다.
  - **테스트에서 `datetime` 모듈을 mock/patch로 가짜 시간으로 바꾸기** — 거부 이유: 입문자에게 mocking·patch는 개념이 낯설고, pytest의 monkeypatch 사용법까지 추가로 배워야 해서 지금 단계에 비해 복잡하다.
- **결과**:
  - 장점: 테스트에서 원하는 날짜를 자유롭게 넣어 항상 같은 결과가 나오는 재현 가능한 테스트를 작성할 수 있고, 함수가 외부 상태에 의존하지 않는 순수 함수에 가까워져 동작을 이해하기 쉽다.
  - 단점: 실제로 앱을 실행하는 코드(`main` 등)에서는 매번 `date.today()`를 호출해 `today` 인자로 넘겨줘야 하는 한 단계가 추가되고, 함수 시그니처에 인자가 하나 더 늘어난다.

## ④ 안 만들 것

| 항목 | 안 만드는 이유 |
|---|---|
| 웹 화면 | CLI 도구로 범위를 좁혀 입문자도 다루기 쉽게 하기 위해 |
| 회원가입 / 로그인 | 나 혼자 쓰는 도구라 계정 구분이 필요 없어서 |
| 발음 듣기 | 핵심 기능(단어 암기·퀴즈)과 무관한 부가 기능이라서 |
| 모바일 앱 | CLI 도구가 목표이며 플랫폼 확장은 이번 범위 밖이라서 |
| 여러 사람 같이 쓰기 (멀티유저) | 혼자 쓰는 도구로 정의했기 때문에 |
| RAG 검색 | LLM 사용 지점을 퀴즈 생성 한 곳으로 한정했기 때문에 |
